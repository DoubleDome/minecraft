# Loaded with override:true when TARGET=test (or `node index.js test`).
# Only set vars you want to differ from .env. Anything you don't set here
# falls through to whatever .env defines.

# Point the generator at a sandbox dir so test runs can't clobber the live pack.
BASE_PATH=""
